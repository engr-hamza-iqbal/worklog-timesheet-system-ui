[build]
  command = "npm run build"
  publish = "dist"

# SPA Fallback for React Router (Login, Register, Dashboard, etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
